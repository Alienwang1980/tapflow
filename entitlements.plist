<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE plist PUBLIC "-//Apple//DTD PLIST 1.0//EN" "http://www.apple.com/DTDs/PropertyList-1.0.dtd">
<plist version="1.0">
<dict>
    <!-- Hardened runtime (required for notarization). These three are the
         py2app-recommended set: Python may allocate executable memory,
         bundle .so/.dylib files are unsigned, launchd injects env vars. -->
    <key>com.apple.security.cs.allow-unsigned-executable-memory</key>
    <true/>
    <key>com.apple.security.cs.disable-library-validation</key>
    <true/>
    <key>com.apple.security.cs.allow-dyld-environment-variables</key>
    <true/>
    <!-- Hardened runtime: without this entitlement the mic permission request
         is rejected at the system level (AVAuthorizationStatus Restricted,
         no prompt, no TCC entry ever written). Observed 2026-08-16 on the
         deployed bundle after TCC reset. -->
    <key>com.apple.security.device.audio-input</key>
    <true/>
</dict>
</plist>
