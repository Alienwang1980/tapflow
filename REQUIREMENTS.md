# Smart Touch Panel — 项目需求文档 v2.0

> 最后更新: 2026-07-07
> 本文件是所有需求的单一事实来源。memory 文件为辅助。

---

## 架构

### 无限画布 + 视口框模型

```
┌──────────────────────────────────────┐
│         无限画布 (infinite canvas)     │
│    ┌──────────────┐                  │
│    │ 设备框(居中)  │ ← iPad 可见区域   │
│    │ iPad 11"     │                  │
│    └──────────────┘                  │
│  框外按钮存在但 iPad 上不可见          │
└──────────────────────────────────────┘
```

- **画布**: 无限方格，每格 = 1 个按钮标准单元
- **按钮**: 放在画布任意 `(col, row)` 坐标，无边界限制
- **设备框**: 始终固定在编辑器正中央，代表目标设备屏幕
- **缩放**: Zoom in/out → cellSize 变化 → 框内格子密度变化
- **平移**: 拖拽画布 → 框相对画布移动 → 框内可见内容变化
- **iPad 显示**: 框内 = 所见即所得，不滚动不缩放

### 数据模型
```json
{
  "profileName": "Default",
  "device": "iPad 11\"",
  "deviceWidth": 834, "deviceHeight": 1210,
  "cellSize": 60,
  "canvasX": 0, "canvasY": 0,
  "defaultSound": "click",
  "windowRules": [...],
  "pages": [{
    "id": "main", "label": "Main",
    "keys": [{
      "id": "k1", "label": "A",
      "col": 0, "row": 0, "w": 1, "h": 1,
      "action": "key", "value": "A",
      "color": "#0f3460", "sound": ""
    }]
  }]
}
```

---

## 需求清单

### R1 — 设备预设

| ID | 需求 | 优先级 |
|----|------|--------|
| R1.1 | 编辑器顶部设备选择器下拉 | P0 |
| R1.2 | 预设设备按宽高比分组：iPad 4:3 (1024×1366), iPad 11" (834×1210), iPad mini (744×1133), Android 16:10 (1280×800) | P0 |
| R1.3 | 同比例设备共用一个框（如 Pro 13" 和 Air 13" 都用 iPad 4:3） | P0 |
| R1.4 | 默认设备 = iPad 11" | P0 |
| R1.5 | 切换设备 → 框比例变化，画布按钮位置不变 | P0 |

### R2 — 画布与缩放

| ID | 需求 | 优先级 |
|----|------|--------|
| R2.1 | 无限画布：按钮可放在任意 `(col, row)`，无 cols/rows 上限 | P0 |
| R2.2 | 框固定在编辑器中央，拖拽移动的是画布 | P0 |
| R2.3 | 滚轮缩放：cellSize 范围 20-200，每格 ±5 | P0 |
| R2.4 | 缩放时框大小不变，格子视觉大小变化 | P0 |
| R2.5 | iPad 端框内区域 = 所见即所得 | P0 |

### R3 — 网格可视化

| ID | 需求 | 优先级 |
|----|------|--------|
| R3.1 | 编辑器显示虚线网格背景（cellSize 间距） | P0 |
| R3.2 | 虚线清晰可见（深色背景上足够对比度） | P0 |
| R3.3 | 格子上不显示坐标数字 | P1 |
| R3.4 | 按钮上显示尺寸标签（如 2×1） | P1 |

### R4 — 按钮操作

| ID | 需求 | 优先级 |
|----|------|--------|
| R4.1 | 拖拽按钮到空格子 → 移动 | P0 |
| R4.2 | 拖拽按钮到有按钮的格子 → 两个按钮交换位置和大小 | P0 |
| R4.3 | 拖拽右下角手柄 → 调整 w×h | P0 |
| R4.4 | 双击空格子 → 在该位置新增按钮 | P0 |
| R4.5 | 按钮属性面板：Label, Action, Value, Color, Sound, w/h(±步进器) | P0 |
| R4.6 | w/h 和 zoom 控件的步进器用 ± 按钮，不用数字输入框 | P0 |

### R5 — iPad 触控面板

| ID | 需求 | 优先级 |
|----|------|--------|
| R5.1 | iPad 全屏 = 设备框内区域 | P0 |
| R5.2 | 按钮按 cellSize 映射到实际像素，保持宽高比 | P0 |
| R5.3 | 多点触控支持 | P0 |
| R5.4 | 手指滑动时按键高亮跟随（touchmove 跟踪） | P0 |
| R5.5 | 不重复发送按键（防止 click + touch 双重触发） | P0 |
| R5.6 | profile 切换下拉 | P1 |
| R5.7 | QR 码显示 | P1 |

### R6 — 音效

| ID | 需求 | 优先级 |
|----|------|--------|
| R6.1 | 全局默认音效（profile 级别） | P1 |
| R6.2 | 按钮单独音效（覆盖全局） | P1 |
| R6.3 | 预置 5 种音效：None, Click, Soft, Mechanical, Deep | P1 |
| R6.4 | Web Audio API 合成，无需音频文件 | P1 |
| R6.5 | 编辑器可试听音效 | P1 |

### R7 — Mac 应用

| ID | 需求 | 优先级 |
|----|------|--------|
| R7.1 | 菜单栏图标（pystray），无 Dock 图标 | P0 |
| R7.2 | Tray 菜单：Open Editor, Health, Quit | P0 |
| R7.3 | py2app 打包为 .app | P0 |
| R7.4 | 开机自启 | P2 |

### R8 — 窗口自动切换

| ID | 需求 | 优先级 |
|----|------|--------|
| R8.1 | 监控前台 app 切换 | P1 |
| R8.2 | 匹配 windowRules → 自动切换 profile page | P1 |
| R8.3 | 通过 WebSocket 广播切换指令到 iPad | P1 |

### R9 — 按键绑定（延后）

| ID | 需求 | 优先级 |
|----|------|--------|
| R9.1 | 键盘捕获：点"捕获"→ 按物理键盘 → 自动填值 | P2 |
| R9.2 | 组合键：modifier checkbox（⌘⌥⌃⇧）+ 主键选择 | P2 |
| R9.3 | 宏录制：录制按键序列 + 时间间隔 + 回放 | P2 |

### R10 — 视觉增强（延后）

| ID | 需求 | 优先级 |
|----|------|--------|
| R10.1 | 按钮背景图片 | P3 |
| R10.2 | 循环动画 | P3 |
| R10.3 | 标准尺寸模板 | P3 |

---

## 当前实现审计

### ✅ 已实现 (P0/P1)
| ID | 状态 | 备注 |
|----|------|------|
| R1.1-R1.5 | ✅ | 设备选择器 + 4预设 + onDeviceChange |
| R2.1-R2.5 | ✅ | 无限画布 + 框居中 + zoomBy + pan |
| R3.1-R3.4 | ✅ | 虚线网格 + 无坐标标签 + 尺寸徽章 |
| R4.1-R4.6 | ✅ | 拖拽移动/交换 + resize + 双击添加 + 属性面板 + 步进器 |
| R5.1-R5.7 | ✅ | iPad 绝对定位渲染 + 多点触控 + touchUsed + 滑动跟踪 + QR |
| R6.1-R6.5 | ✅ | 全局/按钮音效 + Web Audio + 试听按钮 |
| R7.1-R7.3 | ✅ | pystray + .app 打包 |
| R8.1-R8.3 | ✅ | NSWorkspace 监控 + windowRules 匹配 + WS 广播 |

### ❌ 未实现/有 Bug
| ID | 问题 | 严重度 |
|----|------|--------|
| - | canvasX/Y 单位不一致：编辑器以 cellSize=60 归一化存储，iPad 直接当 grid cell 用，导致 pan 后保存 → iPad 按钮在屏幕外 | **Critical** |
| R7.4 | 开机自启未实现 | P2 |
| R9 | 按键绑定全部延后 | P2 |
| R10 | 视觉增强全部延后 | P3 |

### ⚠️ 低优先级问题
- resize handle 12px 偏小
- 缩放时画布中心点可能跳动
- 无撤销/重做
- 无键盘快捷键
- sound test 按钮不解析继承
- 无 visualViewport resize 处理

---

## 待修 Bug（本迭代）

唯一 Critical bug：**canvasX/Y 单位不统一**

**根因**: 编辑器 pan handler 存 `canvasX += deltaPx/60`（归一化到 cellSize=60），iPad 渲染器直接用 `canvasX` 当 grid cell 偏移，导致按钮位置完全错误。

**修法**: 统一存储为 grid cell 单位（浮点），不归一化。编辑器 pan: `canvasX += deltaPx/cellSize`。iPad 渲染: `x = ox + (col - canvasX) * cellSize * scale`。

