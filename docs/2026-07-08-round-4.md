# STP 第四轮修复计划 — 2026-07-08 R4

> 8项修复 | 3 Phase | 完整代码预演 | 三审通过
> 父: `跨平台智能触控输入面板-实施计划.md`

---

## 代码现状（已逐行核实）

| L | 内容 | 问题 |
|----|------|------|
| 37 | `.gh` CSS `display:none` | 正确，JS控制 |
| 76 | gh/gtip HTML | 正确 |
| 141 | rgrp + saveAsGroup | 函数正确 |
| 143 | 宏JS + ungroupById | 挤同一行，功能正常 |
| 228 | backgroundSize | 缺 backgroundPosition |
| 350-365 | hover | gh2变量名冲突，gtip赋值bug |
| 414 | 多选面板 | 缺Save as Group按钮，Border max=20 |
| 422 | Action下拉 | 5选项UTF-8乱码 |
| 426 | Record Macro按钮 | 需删，改用宏modal |

---

## Phase 1: G1 + G2

### G1 — 网格跟随pan

**L228 before:**
```js
canvas.style.backgroundSize=gridPx+"px "+gridPx+"px";
```
**after:**
```js
canvas.style.backgroundSize=gridPx+"px "+gridPx+"px";
canvas.style.backgroundPosition=((-(panX*cpx)%gridPx)+gridPx)%gridPx+"px "+((-(panY*cpx)%gridPx)+gridPx)%gridPx+"px";
```
`((v%g)+g)%g` 保证取模结果为正数。

### G2 — hover高亮

**L358-362 before:**
```js
const csw=cpx*scale;const gh2=document.getElementById("gh");
gh2.style.display="block";gh2.style.left=(csx-crect.left)+"px";
gh2.style.top=(csy-crect.top)+"px";gh2.style.width=csw+"px";gh2.style.height=csw+"px";
const gt2=document.getElementById("gtip").style.display="block";
```
**after:**
```js
const csw=cpx*scale;const gh=document.getElementById("gh");
gh.style.display="block";gh.style.left=(csx-crect.left)+"px";
gh.style.top=(csy-crect.top)+"px";gh.style.width=csw+"px";gh.style.height=csw+"px";
document.getElementById("gtip").style.display="block";
```
`const gt2=...="block"` bug: 把 `"block"` 字符串赋给 gt2 而非设置元素样式 → 改为直接 `document.getElementById("gtip").style.display="block"`。

| 文件: editor.html | 重启: 无 |

---

## Phase 2: G3 + G4 + G5 + G6

### G3 — Save as Group按钮

**L414 多选面板末尾 before:**
```js
+ "<div class=\"br\" style=\"margin-top:8px\"><button class=\"btn-del\" onclick=\"delSelected()\">Delete Selected</button></div>";
```
**after:**
```js
+ "<button onclick=\"saveAsGroup()\" style=\"width:100%;padding:5px;margin:4px 0;font-size:11px;cursor:pointer;border-radius:3px;background:rgba(233,161,69,0.3);color:#e9a145;border:1px solid #e9a145\">Save as Group</button>"
+ "<div class=\"br\" style=\"margin-top:4px\"><button class=\"btn-del\" onclick=\"delSelected()\">Delete Selected</button></div>";
```

### G4 — 编组删除

**现状确认**: `ungroupById` (L143末尾) 函数正确，`renderAll()` 已调 `rgrp()`。无需代码修改。

### G5 — 多选圆角max

**L414 Border行 before:**
```js
"<label>Border (all)</label><input type=range min=0 max=20 value=5 onchange=\"upkGroup('borderRadius',parseInt(this.value))\">"
```
**after:**
```js
"<label>Border (all)</label><input type=range min=0 max='+Math.floor(cs/2)+' value=5 onchange=\"upkGroup('borderRadius',parseInt(this.value))\">"
```

### G6 — Action下拉

**L422 before (5选项, UTF-8乱码):**
```js
'<select onchange="upk(\'action\',this.value)"><option value="key"...>正则按(Press)</option><option value="hold"...>保持(Hold)</option><option value="turbo"...>连击(Turbo)</option><option value="text"...>文本(Text)</option><option value="macro"...>宏(Macro)</option></select>'
```
**after (2选项, 纯英文, 兼容旧key):**
```js
'<select onchange="upk(\'action\',this.value)"><option value="hold"'+(key.action==="hold"||key.action==="key"?" selected":"")+'>Hold (long press)</option><option value="turbo"'+(key.action==="turbo"?" selected":"")+'>Turbo (rapid fire)</option></select>'
```
`key.action==="key"` 兼容旧模板中仍用 `action:"key"` 的按键。

| 文件: editor.html | 重启: 无 |

---

## Phase 3: G7 + G8

### G8 — 删旧宏入口

**L426 before:**
```js
'<button onclick="openMM(' + "'" + selKey + "'" + ')" ...>Record Macro</button><div class="br"...
```
**after:**
```js
'<div class="br" style="margin-top:4px"><button class="btn-del" onclick="dkey()">Delete</button></div>';
```

### G7 — 宏录制重做

#### openMM — 加载已保存值

**L143 before:**
```js
function openMM(kid){mTK=kid;mT=[];var k=cp_()?.keys.find(...);document.getElementById("mkl").textContent="Recording: "+(k?k.label:kid);setMM(1);renderMT();...}
```
**after:**
```js
function openMM(kid){mTK=kid;mT=[];var k=cp_()?.keys.find(function(x){return x.id===kid});if(k&&k.action==="macro"&&k.value){var sep=k.value.indexOf("+")>=0?"+":",";mT=k.value.split(sep);setMM(sep==="+"?1:2)}else{setMM(1)}document.getElementById("mkl").textContent=(k?k.label:kid)+(mT.length?" (edit)":" (new)");renderMT();document.getElementById("macroModal").style.display="flex"}
```

#### renderMT — tag加拖拽

每个tag span加:
```
draggable="true" ondragstart="mTDrag(event,i)"
```

新增函数:
```js
function mTDrag(e,i){e.dataTransfer.setData("text/plain",i)}
function mTDrop(e){e.preventDefault();var f=parseInt(e.dataTransfer.getData("text/plain"));var t=e.target.closest("[data-mi]");if(!t)return;t=parseInt(t.getAttribute("data-mi"));if(f===t)return;var item=mT.splice(f,1)[0];mT.splice(t,0,item);renderMT()}
```

#### modal容器加ondrop

`#mtc` 加 `ondrop="mTDrop(event)"`:
```html
<div id="mtc" ... ondrop="mTDrop(event)">
```

| 文件: editor.html | 重启: 无 |

---

## 修改汇总

| # | L | 改动 | 风险 |
|---|----|------|------|
| G1 | 228 | +1行 backgroundPosition | 低 |
| G2 | 358-364 | 变量名+赋值修复 | 低 |
| G3 | 414 | +Save as Group按钮 | 低 |
| G4 | — | 无需改 | — |
| G5 | 414 | max=20→Math.floor(cs/2) | 低 |
| G6 | 422 | action 5→2选项 | 中 |
| G7 | 143+76 | openMM加载+拖拽+容器 | 中 |
| G8 | 426 | -1行 | 低 |

---

## 三审

V1: G7 L143拆分、G1负数取模、G6兼容旧action
V2: 三项已修复
V3: G2 gh变量let块作用域不冲突。无新问题。通过。
