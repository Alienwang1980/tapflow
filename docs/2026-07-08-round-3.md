# STP 第三轮计划 — 2026-07-08 R3

> 需求: B1-r3 + B4-B6 + R1-R7 | 4 Phase | 已读代码 | 三审
> 父: `跨平台智能触控输入面板-实施计划.md`

---

## 代码现状（已核实）

| 区域 | 当前状态 | 问题 |
|------|---------|------|
| ftb | `rr()` 中无 `ftb.style.*` 定位代码 | B1-r3: ftb 在左上角 |
| `.grouped` | `rr()` 第26行: `const grp=k.groupId?" grouped":"";` | B4: 需删除 |
| Group按钮 | toolbar 第64-65行 | B4: 需删除 |
| `gh`/`gt` | 文字乱码(UTF-8→latin1) | B6: 改英文 |
| action下拉 | 仍为 "Key Press"/"Type Text" | R5: 改中文名 |
| dblclick | 直接建key,无弹窗 | R4: 加modal |
| 编组 | `groupSelected()`等存在 | R1: 移入侧边栏 |
| 宏 | `buildCombo()` checkbox方案 | R2: 重做tag系统 |
| 模板 | 所有key `action:"key"` | R6: 分配hold/turbo |

---

## Phase 1: Bug修复+小改动

### B1-r3 — ftb移框角
在`rr()`中`canvas.style.width`后插入:
```js
const ox=(cw-dw*scale)/2,oy=(ch-dh*scale)/2;
const ftb=document.getElementById("ftb");
ftb.style.right=ox+"px";ftb.style.top=Math.max(0,oy-22)+"px";
ftb.style.left="auto";ftb.style.width="auto";
```

### B4 — 去编组视觉
删: `.grouped` CSS行、`--group:#e9a145;`、`.btn-group`+`.sep` CSS、toolbar Group/Ungroup按钮+sep
改: `rr()`中 `const grp=...` → 删, `"ck"+sel+grp` → `"ck"+sel`

### B5 — hover中心锚点
当前代码已正确(格覆盖鼠标所在snap cell)。无需改坐标公式。

### B6 — 英文提示+红框外
`gt` 改英文: `Double-click empty area to add key`
`gtip` 改为 `position:fixed;bottom:24px;left:50%;transform:translateX(-50%)` (视口底部)

### R5 — Action名称
```html
<option value="key">常规按键 (Press)</option>
<option value="hold">长按保持 (Hold)</option>
<option value="turbo">连续点击 (Turbo)</option>
<option value="text">输入文本 (Text)</option>
<option value="macro">宏按键 (Macro)</option>
```

### R7 — 圆角slider max=cs/2
rpr()中Style区加borderRadius range slider, max动态=`cs/2`

| 重启 | 无 |

---

## Phase 2: 编辑器视觉

### R3 — 背景网格线
rr()中canvas上画CSS网格:
```js
canvas.style.backgroundImage="linear-gradient(rgba(255,255,255,0.04) 1px,transparent 1px),linear-gradient(90deg,rgba(255,255,255,0.04) 1px,transparent 1px)";
canvas.style.backgroundSize=(cs+gp)+"px "+(cs+gp)+"px";
```

### R4 — 双击弹窗
新增modal HTML(`</body>`前)+ `addKeyOfType()`function
dblclick→记录col/row→弹窗→选类型→确认→建key

| 重启 | 无 |

---

## Phase 3: 编组侧边栏+宏

### R1 — 编组侧边栏
右侧面板加Groups section + `rgrp()`/`saveAsGroup()`/`selectGroup()`/`ungroupById()`
点组名=重置选中+选该组全部(Shift可追加)
`renderAll()`加`rgrp()`调用

### R2 — 宏录制弹窗
Modal: tag容器(draggable)+⌘⌥⌃⇧按钮+Combo/Sequence模式
keydown→新建tag(label标签)→拖拽排序→×删除
保存: combo用`+`, sequence用`,`连接

| 重启 | 无 |

---

## Phase 4: 数据

### R6 — 模板action
`_default_template.json`: modifier键→hold, 其他→turbo
`profile_manager.py`: `key.setdefault("action","turbo")`

| 重启 | server |

---

## 进度

| Phase | 内容 | 状态 |
|-------|------|------|
| 1 | B1-r3+B4+B5+B6+R5+R7 | ⬜ |
| 2 | R3+R4 | ⬜ |
| 3 | R1+R2 | ⬜ |
| 4 | R6 | ⬜ |

---

## 三审

V1: R2 keydown需{capture:true}拦截; R1 ungroupById需处理空group
V2: 修复; V3: 无新问题。通过。
